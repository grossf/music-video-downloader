version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # One pull request per week for all actions, not one per action.
    groups:
      actions:
        patterns: ["*"]

  - package-ecosystem: pip
    directory: /server
    schedule:
      interval: weekly
    groups:
      python:
        patterns: ["*"]

  # The python base image. yt-dlp itself is updated at container start.
  - package-ecosystem: docker
    directory: /server
    schedule:
      interval: weekly
    # Stay on the Python version CI tests against; moving to a newer one is a
    # deliberate change to the workflow and the image together.
    ignore:
      - dependency-name: python
        update-types: ["version-update:semver-major", "version-update:semver-minor"]
